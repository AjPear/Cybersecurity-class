#!/bin/bash

if [$4 -e Roulette]; then
awk '{print $1, $2, $5, $6}' "$1"_Dealer_schedule | grep -w "$2" | grep -w "$3" | 


if [$4 -e Roulette]; then
awk '{print $1, $2, $5, $6}' "$1"_Dealer_schedule | grep -w "$2" | grep -w "$3" |   
